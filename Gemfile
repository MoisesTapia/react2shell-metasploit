# frozen_string_literal: true

source 'https://rubygems.org'

# Testing framework - install manually if needed:
# gem install rspec
gem 'rspec'

# Property-based testing - install manually if needed:
# gem install rantly  
gem 'rantly'

# Development dependencies
group :development do
  gem 'rubocop'
end

# Note: In actual Metasploit usage, all dependencies are provided by the framework
# This Gemfile is only for standalone development and testing